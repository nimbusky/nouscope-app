import { Layout } from "@/components/layout/Layout";

import type { NextPage } from "next";

const Goal: NextPage = () => {
  return (
    <Layout title="Goal page">
      <h1>ここは目標管理ページです。</h1>
    </Layout>
  );
};

export default Goal;
