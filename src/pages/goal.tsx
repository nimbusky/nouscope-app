import Head from "next/head";

import type { NextPage } from "next";

const Goal: NextPage = () => {
  return (
    <div>
      <Head>
        <title>Goal Page</title>
      </Head>
      <main>
        <h1>ここは目標設定ページです</h1>
      </main>
    </div>
  );
};

export default Goal;
