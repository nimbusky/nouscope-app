import { Layout } from "@/components/layout/Layout";

import type { NextPage } from "next";

const Personality: NextPage = () => {
  return (
    <Layout title="Personality page">
    <h1>ここは性格分析ページです。</h1>
  </Layout>
  );
};

export default Personality;
